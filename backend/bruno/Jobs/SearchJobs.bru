meta {
  name: SearchJobs
  type: http
  seq: 1
}

get {
  url: {{base_url}}/jobs/search
  body: none
  auth: inherit
}

params:query {
  ~keywords: typescript
}

headers {
  Cookie: {{sessionCookie}}
}

settings {
  encodeUrl: true
  timeout: 0
}
